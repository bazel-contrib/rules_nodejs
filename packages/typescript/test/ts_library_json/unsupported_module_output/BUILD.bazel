load("//packages/typescript:index.bzl", "ts_library")

ts_library(
    name = "unsupported_module_output",
    srcs = [
        "data.json",
        "import_json.ts",
    ],
    allow_json_srcs = True,
    expected_diagnostics = [
        "TS5071: Option '--resolveJsonModule' can only be specified when module code generation is 'commonjs', 'amd', 'es2015' or 'esNext'.",
    ],
    tsconfig = ":tsconfig.json",
)
