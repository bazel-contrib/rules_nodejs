{
    "private": true,
    "devDependencies": {
        "jasmine": "3.4.0",
        "parcel-bundler": "1.12.3"
    },
    "scripts": {
        "test": "bazel run @npm//parcel-bundler/bin:parcel -- --help && bazel test //..."
    }
}
