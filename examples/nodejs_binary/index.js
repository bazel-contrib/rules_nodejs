import {printName} from './lib';
import {internalFile} from 'someLibrary';
import {internalFileFlow} from 'flowLibrary';

printName('Bazel');
console.log(`internalFile: ${internalFile}`);
console.log(`internalFileFlow: ${internalFileFlow}`);
