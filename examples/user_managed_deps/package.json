{
  "description": "runtime dependencies for program example",
  "dependencies": {
    "lodash": "4.17.20"
  },
  "scripts": {
    "pretest": "bazel run @nodejs_host//:yarn -- install",
    "test": "bazel test ..."
  }
}
