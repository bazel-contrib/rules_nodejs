{
  "description": "runtime dependencies for program example",
  "devDependencies": {
    "jasmine": "~3.4.0",
    "jasmine-core": "~3.4.0",
    "v8-coverage": "1.0.9"
  },
  "scripts": {
    "pretest": "bazel run @nodejs//:yarn_node_repositories",
    "test": "bazel test ..."
  }
}
