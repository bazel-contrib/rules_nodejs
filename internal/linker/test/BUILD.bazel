load("//internal:tsc.bzl", "tsc")

tsc(
    name = "test_lib",
    srcs = glob(["*.ts"]),
    deps = [
        "//internal/linker:linker_lib",
        "//packages/runfiles:bazel_runfiles",
        "@npm//@types/jasmine",
        "@npm//@types/node",
    ],
)
