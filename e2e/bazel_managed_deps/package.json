{
  "description": "runtime dependencies for bazel_managed_deps example",
  "devDependencies": {
    "@bazel/jasmine": "^1.2.4",
    "typescript": "^3.0.1"
  },
  "scripts": {
    "test": "bazel test ..."
  }
}
